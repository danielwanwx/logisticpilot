# Approved dashboard reference

These are the user's Claude-produced reference screenshots, copied without
alteration from MedGuard's `design-reference/miss20-dashboard/` on September 13,
2026. The user explicitly selected this design for LogisticPilot and reiterated
that selection after the photo-intake UI became too report-like.

Preserve their composition, density, typography, spacing and green/neutral
palette. The screenshot quantities and example historical benchmarks are design
content, not a source of current application data. Render today's connected
case through the existing backend instead. Detailed evidence belongs behind a
deliberate click, not repeated across the dashboard.
